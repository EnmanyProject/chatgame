# Git 별칭 설정 (비용 절감용)
# 사용법: git config --local include.path ../.gitaliases

[alias]
    # 빠른 상태 확인
    s = status -s
    st = status -s
    
    # 간단한 로그
    l = log --oneline -10
    ll = log --oneline -20
    last = log --oneline -1
    
    # 조용한 동기화
    pullq = pull --quiet --no-edit
    pushq = push --quiet
    
    # 빠른 diff
    d = diff --stat
    dc = diff --stat --cached
    
    # 빠른 커밋
    cm = commit -m
    ca = commit -am
    
    # 전체 동기화 (조용히)
    sync = !git pull --quiet --no-edit && git push --quiet
    
    # 상태 요약
    info = !echo "Branch:" && git branch --show-current && echo "Status:" && git status -s && echo "Last commit:" && git log --oneline -1